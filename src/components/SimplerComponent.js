import React from 'react';
import index from '../index'

export const SimplerComponent = (props) => {
    constructor(props)
    super(props)


    return (
        <div onClick={this.props.handleClick} >
            I am just happy
        </div>
    
    )
}

